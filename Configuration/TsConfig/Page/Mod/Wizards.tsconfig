mod.wizards.newContentElement.wizardItems.media {
  elements {
    photographer {
      iconIdentifier = content-photographer
      title = LLL:EXT:photographer/Resources/Private/Language/locallang_db.xlf:plugin.title
      description = PhotoSwipe Galerie (with user selection of images)
      tt_content_defValues.CType = photographer
    }
  }
  show := addToList(photographer)
}
